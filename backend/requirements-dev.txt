# Herramientas de prueba. No van en la imagen: `requirements.txt` es lo que
# necesita la API para correr, esto es lo que necesita quien la desarrolla.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0,<10.0
pytest-cov>=5.0,<8.0

# Las pruebas de caracterización hablan con la API por HTTP, igual que el POS.
requests>=2.31,<3.0
